= PROMPT_REPRISE — Session 003 — Graphify Gradle
:author: cheroliv + opencode (deepseek-v4-pro)
:date: 2026-05-11
:session: 003

[abstract]
Amorçage pour la prochaine session opencode. GF-2 à attaquer.

== Contexte de Reprise (EAGER)

=== Ce qui a été accompli — Session 002-cleanup (11 mai 2026)

- **Nettoyage nested leak** : suppression `graphify-plugin/foundry/OSS/graphify-gradle/graphify-plugin/`
- **Conversion Pattern A** : graphify-plugin + api-key-pool = builds indépendants (settings.gradle.kts + gradlew chacun)
- **api-key-pool publishable** : maven-publish + signing configuré
- **Root consumer-only** : apply plugin only, scanWorkspace task
- **GF-1+ Tests** : 30 tests graphify-plugin (24 task + 6 model), 66 tests api-key-pool, 96/96 total PASS
- **Tests .agents/ cross-project** : 3 nouveaux tests pour références multi-subproject

=== Architecture actuelle — Pattern A (convention foundry)

[source]
----
graphify-gradle/                     # Consumer (dogfooding)
├── build.gradle.kts                 # apply plugin: com.cheroliv.graphify
├── settings.gradle.kts              # NO include()
├── gradlew + gradle/                # Wrapper consumer
├── .agents/                         # GOUVERNANCE UNIQUE
├── graphify-plugin/                 # BUILD INDÉPENDANT
│   ├── settings.gradle.kts          # rootProject.name = "graphify-plugin"
│   ├── gradlew + gradle/            # Wrapper INDÉPENDANT
│   └── src/                         # 30 tests ✅
└── api-key-pool/                    # BUILD INDÉPENDANT
    ├── settings.gradle.kts          # rootProject.name = "api-key-pool"
    ├── gradlew + gradle/            # Wrapper INDÉPENDANT
    └── src/                         # 66 tests ✅, publishable
----

== Mission du Jour — Session 003

=== Objectif prioritaire : GF-2 — Tests intégration

. Scanner le workspace réel (`/home/cheroliv/workspace/`)
. Valider structure graph.json produite (nœuds, arêtes, communautés)
. Vérifier que les 6 boroughs + CSS apparaissent comme communautés
. Vérifier exclusion effective dans un scan réel
. Mesurer performance (taille graph.json, temps de scan)

=== Objectifs secondaires

- Chaque build indépendant doit compiler avec son propre gradlew
- Viser 5+ tests intégration

== Rappel — Règles Globales (EAGER)

. Règle 0 : publishToMavenLocal OBLIGATOIRE après modif code source
. Règle 1 : Aucun commit sans permission explicite
. Règle 1b : Backup avant modif fichier config non versionné
. Règle 2 : Pas de ./gradlew test en fin de session sans permission
. Règle 4bis : DAG inter-plugins N0→N1→N2→N3 — graphify est N0

[.text-right]
*PROMPT_REPRISE mis à jour le 11 mai 2026 — Fin session 002-cleanup.*
*Prochaine session : 003 — GF-2 Tests intégration.*
